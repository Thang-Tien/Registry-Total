import InspectionsTable from "../../Inspection/InspectionTable";

const CenterInspections = (props) => {
  return <InspectionsTable centerId={props.centerId} />;
};

export default CenterInspections;
